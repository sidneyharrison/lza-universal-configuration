{
  "Version": "2012-10-17",
  "Statement": [
    {
      "Action": "s3:GetObject",
      "Effect": "Allow",
      "Resource": [
        "arn:${PARTITION}:s3:::aws-ssm-${REGION}/*",
        "arn:${PARTITION}:s3:::aws-windows-downloads-${REGION}/*",
        "arn:${PARTITION}:s3:::amazon-ssm-${REGION}/*",
        "arn:${PARTITION}:s3:::aws-ssm-${REGION}/*",
        "arn:${PARTITION}:s3:::amazon-ssm-packages-${REGION}/*",
        "arn:${PARTITION}:s3:::${REGION}-birdwatcher-prod/*",
        "arn:${PARTITION}:s3:::aws-ssm-distributor-file-${REGION}/*",
        "arn:${PARTITION}:s3:::aws-ssm-document-attachments-${REGION}/*",
        "arn:${PARTITION}:s3:::patch-baseline-snapshot-${REGION}/*",
        "arn:${PARTITION}:s3:::aws-patch-manager-${REGION}-*/*",
        "arn:${PARTITION}:s3:::aws-patchmanager-macos-${REGION}-*/*"
      ]
    }
  ]
}
